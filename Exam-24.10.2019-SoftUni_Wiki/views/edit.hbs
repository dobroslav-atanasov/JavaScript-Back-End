<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Wiki</title>
    <link rel="stylesheet" href="/css/site.css">
</head>

<body>
    <div>
        <aside>
            <div class="center">
                <img class="logo" src="/images/logo.png" alt="logo">
            </div>

            <nav>
                <a href="/">Main Page</a>
                <a href="/all-articles">All Articles</a>
                <form action="/search" method="GET">
                    <input type="text" name="search">
                    <input type="submit" value="Search">
                </form>
                {{#if user}}
                <a href="/create">Create New Article</a>
                {{/if}}
            </nav>
        </aside>
        <div id="container">
            <header>
                {{#if user}}
                <span>Welcome, {{user.username}}!</span>
                <a href="/logout">[Logout]</a>
                {{else}}
                <a href="/login">Login</a>
                <a href="/register">Register</a>
                {{/if}}
            </header>
            <main>
                <h1>Welcome to SoftUni Wiki!</h1>
                {{#if message}}
                <section id="notifications">
                    <p>{{message}}</p>
                </section>
                {{/if}}
                <section>
                    <h2>Edit article
                    </h2>
                    <div class="spacer">
                        {{#if oldBody}}
                        <form action="/edit/{{currentArticleId}}" method="POST">
                            {{else}}
                            <form action="/edit/{{article.id}}" method="POST">
                                {{/if}}
                                <label>Article title</label>
                                <h3>{{article.title}}</h3>
                                <label>Content</label>
                                {{#if oldBody}}
                                <textarea name="description">{{oldBody.description}}</textarea>
                                {{else}}
                                <textarea name="description">{{article.description}}</textarea>
                                {{/if}}
                                <input type="submit" value="Submit">
                            </form>
                    </div>
                </section>
            </main>
            <footer>
                SoftUni Wiki &copy; 2018 - 2019
            </footer>
        </div>
    </div>
</body>

</html>